<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Кристалл - Развлечения</title>
    <link rel="stylesheet" href="/styles.css">

    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #f7f7f7;
            margin: 0;
            padding: 0;
        }

        /* Header Section */
        .event-page-header {
            background-image: linear-gradient(to bottom, rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.7)), url('/images/main.jpg');
            background-size: cover;
            background-position: center;
            padding: 100px 20px;
            text-align: center;
            color: white;
        }

        .event-page-title {
            font-size: 3rem;
            font-weight: 700;
        }

        .event-page-subtitle {
            font-size: 1.2rem;
            font-weight: 400;
            color: #f1f1f1;
        }

        .btn-header {
            margin-top: 20px;
            background-color: #ff6600;
            color: white;
            padding: 12px 30px;
            border: none;
            border-radius: 25px;
            font-size: 1rem;
            font-weight: 600;
            text-transform: uppercase;
            cursor: pointer;
            transition: background-color 0.3s ease-in-out;
        }

        .btn-header:hover {
            background-color: #e05500;
        }

        .event-page-list {
            padding: 40px 0;
            display: flex;
            flex-wrap: wrap;
            gap: 30px;
            justify-content: center;
        }

        .event-card {
            background-color: white;
            border-radius: 20px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            transition: all 0.4s ease;
            max-width: 340px;
            width: 100%;
            position: relative;
        }

        .event-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
        }

        .event-card img {
            width: 100%;
            height: 220px;
            object-fit: cover;
        }

        .event-card-body {
            padding: 20px;
            text-align: center;
        }

        .event-card-title {
            font-size: 1.8rem;
            font-weight: 700;
            margin-bottom: 10px;
            color: #333;
        }

        .event-card-date,
        .event-card-location {
            font-size: 1rem;
            color: #666;
            margin-bottom: 15px;
        }

        .event-card-footer {
            display: flex;
            justify-content: space-between;
            padding: 15px 20px;
            background-color: white;
        }

        .btn-outline-secondary,
        .btn-primary {
            border-radius: 25px;
            padding: 10px 20px;
            font-size: 1rem;
            font-weight: 500;
            transition: background-color 0.3s ease, box-shadow 0.3s ease;
        }

        .btn-primary {
            background-color: #007bff;
            border: none;
            color: white;
        }

        .btn-primary:hover {
            background-color: #0056b3;
        }

        .btn-outline-secondary {
            border: 2px solid #007bff;
            color: #007bff;
        }

        .btn-outline-secondary:hover {
            background-color: #007bff;
            color: white;
        }

        /* Ribbon for "Новое событие" */
        .ribbon {
            position: absolute;
            top: 15px;
            left: -10px;
            background-color: #ff6600;
            color: white;
            padding: 5px 20px;
            font-size: 0.8rem;
            font-weight: bold;
            border-radius: 5px;
            transform: rotate(-15deg);
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.1);
        }

        /* Footer Section */
        .footer {
            background-color: #1a1a1a;
            color: white;
            padding: 40px 20px;
        }

        .footer .footer-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
        }

        .footer .footer-column {
            flex: 1 1 calc(33.333% - 20px);
            margin: 10px 0;
        }

        .footer h4 {
            font-size: 1.5rem;
            margin-bottom: 15px;
        }

        .footer p,
        .footer a {
            color: #ccc;
            font-size: 1rem;
            text-decoration: none;
        }

        .footer a:hover {
            color: #f1c40f;
        }

        .footer .social-icons a {
            color: white;
            font-size: 1.2rem;
            margin-right: 15px;
        }

        .footer .social-icons a:hover {
            color: #f1c40f;
        }

        .footer .footer-bottom {
            margin-top: 30px;
            text-align: center;
            border-top: 1px solid #444;
            padding-top: 20px;
        }
        .breadcrumb {
    font-size: 0.9em;
    color: #ddd;
    margin-bottom: 15px;
    text-align: center;  /* Центровка текста */
    display: inline-block;  /* Позволяет автоцентрировать */
    margin-left: auto;  /* Центровка по горизонтали */
    margin-right: auto; /* Центровка по горизонтали */
}


        @media (max-width: 768px) {
            .event-page-title {
                font-size: 2.2rem;
            }

            .footer .footer-container {
                flex-direction: column;
                align-items: center;
            }

            .footer .footer-column {
                margin-bottom: 20px;
            }
            

        }
    </style>
</head>
<body>
    <!-- Header -->
    <header class="event-page-header">
        <nav class="breadcrumb">
            <a href="/">Главная</a> > <span>Развлечения</span>
        </nav>
        <h1 class="event-page-title">Развлечения</h1>
        <p class="event-page-subtitle">Лучшие мероприятия и события для всей семьи</p>
    </header>

    <!-- Main Content with Event Cards -->
    <main class="container">
        <section class="event-page-section">
            <div class="event-page-list">
                <% events.forEach(event => { %>
                    <div class="event-card">
                        <% if (event.isNew) { %>
                            <div class="ribbon">Новое</div>
                        <% } %>
                        <img src="/images/event<%= event.id %>.jpg" alt="<%= event.title %>" class="event-card-image">
                        <div class="event-card-body">
                            <h3 class="event-card-title"><%= event.title %></h3>
                            <p class="event-card-date"><i class="fas fa-calendar-alt"></i> Дата: <%= event.date %></p>
                            <p class="event-card-location"><i class="fas fa-map-marker-alt"></i> Локация: <%= event.location %></p>
                        </div>
                        <div class="event-card-footer">
                            <a href="/events/<%= event.id %>" class="btn-outline-secondary">Подробнее</a>
                            <button class="btn-primary" onclick="redirectToReservation(event, '<%= event.title %>')">Забронировать</button>
                        </div>
                    </div>
                <% }) %>
            </div>
        </section>

    </main>
<!-- Footer Section -->
<footer class="footer">
    <div class="footer-container">
        <!-- Контакты -->
        <div class="footer-column">
            <h4>Контакты</h4>
            <p><i class="fas fa-map-marker-alt"></i> Брянск, ул. Объездная, д. 30, 32</p>
            <p><i class="fas fa-phone-alt"></i> +7 (123) 456-78-90</p>
            <p><i class="fas fa-envelope"></i> info@kristall.ru</p>
            <p>Режим работы: 10:00 - 22:00</p>
        </div>
        <!-- Социальные сети -->
        <div class="footer-column">
            <h4>Социальные сети</h4>
            <p>Следите за нами:</p>
            <div class="social-icons">
                <a href="#" class="fab fa-facebook-f"></a>
                <a href="#" class="fab fa-instagram"></a>
                <a href="#" class="fab fa-twitter"></a>
                <a href="#" class="fab fa-youtube"></a>
            </div>
        </div>
        <!-- О нас -->
        <div class="footer-column">
            <h4>Полезные ссылки</h4>
            <ul>
                <li><a href="/map">О торговом центре</a></li>
                <li><a href="/vacancies">Вакансии</a></li>
                <li><a href="/rental">Заявка на аренду</a></li>
                <li><a href="/privacy">Политика конфиденциальности</a></li>
            </ul>
        </div>
    
    </div>
    <div class="footer-bottom">
        <hr>
        <p>© 2024 ТРЦ Кристалл | Все права защищены</p>
    </div>
</footer>

<style>
    .footer {
        background-color: #1a1a1a;
        color: white;
        padding: 40px 20px;
    }

    .footer-container {
        display: flex;
        flex-wrap: wrap;
        gap: 20px;
        justify-content: space-between;
    }

    .footer-column {
        flex: 1 1 calc(25% - 20px);
        margin-bottom: 20px;
    }

    .footer h4 {
        font-size: 1.5rem;
        margin-bottom: 15px;
        color: white;
    }

    .footer p,
    .footer a,
    .footer li {
        color: #ccc;
        font-size: 1rem;
        text-decoration: none;
    }

    .footer ul {
        list-style-type: none;
        padding: 0;
    }

    .footer li {
        margin-bottom: 10px;
    }

    .footer a:hover {
        color: #f1c40f;
    }

    .social-icons {
        margin-top: 10px;
    }

    .social-icons a {
        color: white;
        font-size: 1.5rem;
        margin-right: 15px;
        transition: color 0.3s ease;
    }

    .social-icons a:hover {
        color: #f1c40f;
    }

    .subscribe-input {
        width: 100%;
        padding: 10px;
        border: none;
        border-radius: 5px;
        margin-bottom: 10px;
        font-size: 1rem;
    }

    .subscribe-button {
        background-color: #007bff;
        color: white;
        padding: 10px 20px;
        border: none;
        border-radius: 5px;
        font-size: 1rem;
        cursor: pointer;
        width: 100%;
    }

    .subscribe-button:hover {
        background-color: #0056b3;
    }

    .footer-bottom {
        margin-top: 30px;
        text-align: center;
    }

    .footer-bottom hr {
        border-color: #444;
        margin-bottom: 10px;
    }

    .footer-bottom p {
        margin: 0;
    }

    @media (max-width: 768px) {
        .footer-container {
            flex-direction: column;
            align-items: center;
        }

        .footer-column {
            flex: 1 1 100%;
            margin-bottom: 20px;
        }
    }
</style>

    <script>
        function redirectToReservation(e, eventName) {
            e.stopPropagation(); // Предотвращаем всплытие события
            window.location.href = `/reserve?eventName=${encodeURIComponent(eventName)}`;
        }
    </script>
</body>
</html>
